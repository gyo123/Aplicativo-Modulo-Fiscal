<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.modulofiscal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ModuloFiscal">
        <activity android:name=".simplesnacional"></activity>
        <activity android:name=".calculadora" />
        <activity android:name=".DicasLucroPresumido" />
        <activity android:name=".lei116" />
        <activity android:name=".infimpostos" />
        <activity android:name=".tabelaiss" />
        <activity android:name=".Formbemvindo.Agradecimentos" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Slides">
            android:name=".MainIntroActivity"
            android:theme="@style/Theme.Intro"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>